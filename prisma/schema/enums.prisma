enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum UserStatus {
    ACTIVE
    BLOCKED
}

enum PropertyStatus {
    AVAILABLE
    RENTED
    UNAVAILABLE
}

enum RentalRequestStatus {
    PENDING
    APPROVED
    REJECTED
    ACTIVE
    COMPLETED
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
}
